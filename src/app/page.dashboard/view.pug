header(class="sticky top-0 z-20 border-b border-zinc-200 bg-white/95 backdrop-blur dark:border-zinc-800 dark:bg-zinc-950/95")
    div(class="flex flex-col gap-3 px-5 py-4 md:flex-row md:items-center md:justify-between")
        div
            div(class="flex items-center gap-2 text-xs font-medium text-zinc-500 dark:text-zinc-400")
                i(class="fa-solid fa-gauge-high")
                span Dashboard
            h1(class="mt-1 text-xl font-semibold text-zinc-950 dark:text-zinc-50") Docker Infra
        div(class="flex items-center gap-2")
            span(class="inline-flex items-center gap-1.5 rounded-md border px-2.5 py-1.5 text-xs font-semibold", [ngClass]="backupBadgeClass()")
                i(class="fa-solid fa-box-archive text-[11px]")
                | {{backupBadgeText()}}
            span(class="inline-flex items-center gap-1.5 rounded-md border px-2.5 py-1.5 text-xs font-semibold", [ngClass]="statusClass(health()?.status)")
                i(class="fa-solid fa-circle text-[7px]")
                | {{statusLabel(health()?.status)}}
            button(type="button", (click)="load()", class="inline-flex h-9 items-center gap-2 rounded-md border border-zinc-300 bg-white px-3 text-sm font-semibold text-zinc-700 hover:bg-zinc-50 dark:border-zinc-700 dark:bg-zinc-900 dark:text-zinc-200 dark:hover:bg-zinc-800")
                i(class="fa-solid fa-rotate-right")
                span 새로고침

main(class="space-y-6 px-5 py-5")
    div(*ngIf="error()", class="rounded-md border border-rose-200 bg-rose-50 px-4 py-3 text-sm text-rose-700 dark:border-rose-900/70 dark:bg-rose-950/40 dark:text-rose-300") {{error()}}

    div(*ngIf="!error()", class="space-y-6")
        section(class="rounded-md border border-zinc-200 bg-white dark:border-zinc-800 dark:bg-zinc-900")
            div(class="flex flex-col gap-2 border-b border-zinc-200 px-4 py-3 dark:border-zinc-800 md:flex-row md:items-center md:justify-between")
                div
                    h2(class="text-sm font-semibold text-zinc-950 dark:text-zinc-50") 서버 자원 추이
                    p(class="mt-0.5 text-xs text-zinc-500 dark:text-zinc-400") {{resourceRangeText()}}
                div(class="flex flex-wrap items-end gap-2")
                    label(class="block")
                        span(class="mb-1 block text-[11px] font-semibold text-zinc-500 dark:text-zinc-400") 시작일
                        input(type="date", [ngModel]="resourceStartDate()", (ngModelChange)="resourceStartDate.set($event)", class="h-8 rounded-md border border-zinc-300 bg-white px-2 text-xs text-zinc-900 dark:border-zinc-700 dark:bg-zinc-950 dark:text-zinc-100")
                    label(class="block")
                        span(class="mb-1 block text-[11px] font-semibold text-zinc-500 dark:text-zinc-400") 종료일
                        input(type="date", [ngModel]="resourceEndDate()", (ngModelChange)="resourceEndDate.set($event)", class="h-8 rounded-md border border-zinc-300 bg-white px-2 text-xs text-zinc-900 dark:border-zinc-700 dark:bg-zinc-950 dark:text-zinc-100")
                    button(type="button", (click)="applyResourceRange()", [disabled]="resourceChartBusy()", class="inline-flex h-8 items-center gap-2 rounded-md bg-zinc-950 px-2.5 text-xs font-semibold text-white hover:bg-zinc-800 disabled:opacity-50 dark:bg-zinc-100 dark:text-zinc-950 dark:hover:bg-zinc-200")
                        i(class="fa-solid", [ngClass]="resourceChartBusy() ? 'fa-spinner fa-spin' : 'fa-chart-line'")
                        span 조회
                    button(type="button", (click)="openNodeCharts()", class="inline-flex h-8 items-center gap-2 rounded-md border border-zinc-300 px-2.5 text-xs font-semibold text-zinc-700 hover:bg-zinc-50 dark:border-zinc-700 dark:text-zinc-200 dark:hover:bg-zinc-800")
                        i(class="fa-solid fa-chart-area")
                        span 서버별 보기
            div(class="p-4")
                div(*ngIf="resourceChartError()", class="mb-3 rounded-md border border-rose-200 bg-rose-50 px-3 py-2 text-sm text-rose-700 dark:border-rose-900/70 dark:bg-rose-950/40 dark:text-rose-300") {{resourceChartError()}}
                div(*ngIf="resourceChartBusy() && resourceRows().length === 0", class="flex h-64 items-center justify-center gap-2 rounded-md border border-dashed border-zinc-300 text-sm text-zinc-500 dark:border-zinc-700 dark:text-zinc-400")
                    i(class="fa-solid fa-spinner fa-spin")
                    span 자원 기록을 불러오는 중입니다.
                div(*ngIf="!resourceChartBusy() && !resourceChartError() && resourceRows().length === 0", class="flex h-64 items-center justify-center rounded-md border border-dashed border-zinc-300 text-sm text-zinc-500 dark:border-zinc-700 dark:text-zinc-400") 선택한 기간에 표시할 자원 기록이 없습니다.
                div(*ngIf="resourceRows().length > 0", class="grid grid-cols-1 divide-y divide-zinc-100 dark:divide-zinc-800 xl:grid-cols-3 xl:divide-x xl:divide-y-0")
                    div(class="min-h-72 px-2 py-3")
                        h3(class="px-1 text-xs font-semibold text-sky-700 dark:text-sky-300") CPU
                        div(data-resource-chart="cpu", class="mt-2 h-64")
                    div(class="min-h-72 px-2 py-3")
                        h3(class="px-1 text-xs font-semibold text-emerald-700 dark:text-emerald-300") Memory
                        div(data-resource-chart="memory", class="mt-2 h-64")
                    div(class="min-h-72 px-2 py-3")
                        h3(class="px-1 text-xs font-semibold text-amber-700 dark:text-amber-300") Storage
                        div(data-resource-chart="storage", class="mt-2 h-64")

        section(class="grid grid-cols-1 gap-4 xl:grid-cols-[1.1fr_0.9fr]")
            div(class="rounded-md border border-zinc-200 bg-white dark:border-zinc-800 dark:bg-zinc-900")
                div(class="flex items-center justify-between border-b border-zinc-200 px-4 py-3 dark:border-zinc-800")
                    div
                        h2(class="text-sm font-semibold text-zinc-950 dark:text-zinc-50") Servers
                        p(class="mt-0.5 text-xs text-zinc-500 dark:text-zinc-400") {{metricHistoryText()}}
                    a(routerLink="/servers", class="text-xs font-semibold text-sky-700 hover:text-sky-800 dark:text-sky-300") Open
                div(class="divide-y divide-zinc-100 dark:divide-zinc-800")
                    div(*ngIf="isCardLoading('servers')", class="flex items-center justify-center gap-2 px-4 py-8 text-sm text-zinc-500 dark:text-zinc-400")
                        i(class="fa-solid fa-spinner fa-spin")
                        span 서버 정보를 불러오는 중입니다.
                    div(*ngIf="cardError('servers')", class="px-4 py-3 text-sm text-rose-700 dark:text-rose-300") {{cardError('servers')}}
                    div(*ngFor="let node of nodes()", class="flex flex-col gap-3 px-4 py-3 xl:flex-row xl:items-center xl:justify-between")
                        div(class="min-w-0 flex-1")
                            p(class="truncate text-sm font-semibold text-zinc-950 dark:text-zinc-50") {{node.name}}
                            p(class="mt-0.5 truncate text-xs text-zinc-500 dark:text-zinc-400") {{node.role}} · {{node.host}}
                            div(class="mt-2 flex flex-wrap gap-1.5 text-[11px] font-semibold")
                                span(class="rounded-md border border-sky-200 bg-sky-50 px-2 py-0.5 text-sky-700 dark:border-sky-900/70 dark:bg-sky-950/40 dark:text-sky-300") CPU {{nodeMetricPercent(node, 'cpu')}}
                                span(class="rounded-md border border-emerald-200 bg-emerald-50 px-2 py-0.5 text-emerald-700 dark:border-emerald-900/70 dark:bg-emerald-950/40 dark:text-emerald-300") MEM {{nodeMetricPercent(node, 'memory')}}
                                span(class="rounded-md border border-amber-200 bg-amber-50 px-2 py-0.5 text-amber-700 dark:border-amber-900/70 dark:bg-amber-950/40 dark:text-amber-300") DISK {{nodeMetricPercent(node, 'storage')}}
                                span(class="rounded-md border border-zinc-200 bg-zinc-50 px-2 py-0.5 text-zinc-600 dark:border-zinc-700 dark:bg-zinc-950 dark:text-zinc-300") 컨테이너 {{nodeContainerSummary(node).running || 0}}/{{nodeContainerSummary(node).total || 0}}
                        div(class="flex shrink-0 flex-col items-start gap-1 xl:items-end")
                            span(class="inline-flex rounded-md border px-2 py-1 text-xs font-semibold", [ngClass]="statusClass(node.status)") {{statusLabel(node.status)}}
                            span(class="text-[11px] text-zinc-500 dark:text-zinc-400") {{formatDate(nodeMetric(node).reported_at)}}
                    div(*ngIf="!isCardLoading('servers') && !cardError('servers') && nodes().length === 0", class="px-4 py-8 text-center text-sm text-zinc-500 dark:text-zinc-400") No servers

            div(class="rounded-md border border-zinc-200 bg-white dark:border-zinc-800 dark:bg-zinc-900")
                div(class="flex items-center justify-between border-b border-zinc-200 px-4 py-3 dark:border-zinc-800")
                    div
                        h2(class="text-sm font-semibold text-zinc-950 dark:text-zinc-50") 등록된 도메인
                        p(class="mt-0.5 text-xs text-zinc-500 dark:text-zinc-400") Docker Infra 기준 {{domainSummary()?.domain_count || 0}}개
                    a(routerLink="/domains", class="text-xs font-semibold text-sky-700 hover:text-sky-800 dark:text-sky-300") Domains
                div(class="grid grid-cols-3 divide-x divide-zinc-100 border-b border-zinc-200 text-center dark:divide-zinc-800 dark:border-zinc-800")
                    div(class="px-3 py-3")
                        p(class="text-[11px] font-semibold text-zinc-500 dark:text-zinc-400") 도메인
                        p(class="mt-1 text-base font-semibold text-zinc-950 dark:text-zinc-50") {{domainSummary()?.domain_count || 0}}
                    div(class="px-3 py-3")
                        p(class="text-[11px] font-semibold text-zinc-500 dark:text-zinc-400") 연결됨
                        p(class="mt-1 text-base font-semibold text-zinc-950 dark:text-zinc-50") {{domainSummary()?.used_domain_count || 0}}
                    div(class="px-3 py-3")
                        p(class="text-[11px] font-semibold text-zinc-500 dark:text-zinc-400") 서비스
                        p(class="mt-1 text-base font-semibold text-zinc-950 dark:text-zinc-50") {{domainSummary()?.linked_service_count || 0}}
                div(class="divide-y divide-zinc-100 dark:divide-zinc-800")
                    div(*ngIf="isCardLoading('domains')", class="flex items-center justify-center gap-2 px-4 py-8 text-sm text-zinc-500 dark:text-zinc-400")
                        i(class="fa-solid fa-spinner fa-spin")
                        span 도메인 정보를 불러오는 중입니다.
                    div(*ngIf="cardError('domains')", class="px-4 py-3 text-sm text-rose-700 dark:text-rose-300") {{cardError('domains')}}
                    div(*ngFor="let domain of domainRows()", class="flex items-center justify-between gap-3 px-4 py-3")
                        div(class="min-w-0")
                            p(class="truncate text-sm font-semibold text-zinc-950 dark:text-zinc-50") {{domain.domain}}
                            p(class="mt-0.5 truncate text-xs text-zinc-500 dark:text-zinc-400") {{domainServiceSummary(domain)}}
                        span(class="inline-flex shrink-0 rounded-md border px-2 py-1 text-xs font-semibold", [ngClass]="domainBadgeClass(domain)") 서비스 {{domain.service_count || 0}}개
                    div(*ngIf="!isCardLoading('domains') && !cardError('domains') && domainRows().length === 0", class="px-4 py-8 text-center text-sm text-zinc-500 dark:text-zinc-400") 등록된 도메인이 없습니다.

        section(class="rounded-md border border-zinc-200 bg-white dark:border-zinc-800 dark:bg-zinc-900")
            div(class="flex items-center justify-between border-b border-zinc-200 px-4 py-3 dark:border-zinc-800")
                div
                    h2(class="text-sm font-semibold text-zinc-950 dark:text-zinc-50") 최근 작업
                    p(class="mt-0.5 text-xs text-zinc-500 dark:text-zinc-400") 실행 서버와 작업 내용을 함께 표시합니다.
                a(routerLink="/operations", class="text-xs font-semibold text-sky-700 hover:text-sky-800 dark:text-sky-300") 로그 보기
            div(class="divide-y divide-zinc-100 dark:divide-zinc-800")
                div(*ngIf="isCardLoading('operations')", class="flex items-center justify-center gap-2 px-4 py-8 text-sm text-zinc-500 dark:text-zinc-400")
                    i(class="fa-solid fa-spinner fa-spin")
                    span 최근 작업을 불러오는 중입니다.
                div(*ngIf="cardError('operations')", class="px-4 py-3 text-sm text-rose-700 dark:text-rose-300") {{cardError('operations')}}
                div(*ngFor="let operation of operations()", class="flex flex-col gap-2 px-4 py-3 md:flex-row md:items-center md:justify-between")
                    div(class="min-w-0")
                        p(class="truncate text-sm font-semibold text-zinc-950 dark:text-zinc-50") {{operation.action_text || operation.operation_label || operation.type}}
                        p(class="mt-0.5 truncate text-xs text-zinc-500 dark:text-zinc-400") {{operationServerText(operation)}} · {{operationTargetText(operation)}} · {{formatDate(operation.created_at)}}
                        p(*ngIf="operation.message", class="mt-1 line-clamp-2 text-xs leading-5 text-zinc-500 dark:text-zinc-400") {{operation.message}}
                    span(class="inline-flex shrink-0 rounded-md border px-2 py-1 text-xs font-semibold", [ngClass]="statusClass(operation.status)") {{statusLabel(operation.status)}}
                div(*ngIf="!isCardLoading('operations') && !cardError('operations') && operations().length === 0", class="px-4 py-8 text-center text-sm text-zinc-500 dark:text-zinc-400") 작업 이력이 없습니다.

        div(*ngIf="nodeChartsOpen()", class="fixed inset-0 z-40 flex items-start justify-center bg-zinc-950/40 px-4 py-6 backdrop-blur-sm")
            section(class="max-h-[84vh] w-full max-w-5xl overflow-y-auto rounded-md border border-zinc-200 bg-white shadow-xl dark:border-zinc-800 dark:bg-zinc-950")
                div(class="sticky top-0 z-10 flex flex-col gap-2 border-b border-zinc-200 bg-white px-4 py-3 dark:border-zinc-800 dark:bg-zinc-950 md:flex-row md:items-center md:justify-between")
                    div
                        h2(class="text-sm font-semibold text-zinc-950 dark:text-zinc-50") 서버별 자원 추이
                        p(class="mt-0.5 text-xs text-zinc-500 dark:text-zinc-400") {{resourceStartDate()}} ~ {{resourceEndDate()}}
                    button(type="button", (click)="closeNodeCharts()", class="inline-flex h-8 w-8 items-center justify-center rounded-md border border-zinc-300 text-zinc-700 hover:bg-zinc-50 dark:border-zinc-700 dark:text-zinc-200 dark:hover:bg-zinc-900")
                        i(class="fa-solid fa-xmark")
                div(class="p-4")
                    div(*ngIf="nodeResourceCharts().length === 0", class="flex h-48 items-center justify-center rounded-md border border-dashed border-zinc-300 text-sm text-zinc-500 dark:border-zinc-700 dark:text-zinc-400") 서버별 자원 기록이 없습니다.
                    div(*ngIf="nodeResourceCharts().length > 0", class="space-y-4")
                        div(role="tablist", class="flex gap-1 overflow-x-auto rounded-md border border-zinc-200 bg-zinc-50 p-1 dark:border-zinc-800 dark:bg-zinc-900")
                            button(*ngFor="let chart of nodeResourceCharts()", type="button", role="tab", [attr.aria-selected]="isActiveNodeChart(chart)", (click)="selectNodeChart(chart.node_id)", class="min-w-40 shrink-0 rounded-md px-3 py-2 text-left text-xs font-semibold transition", [ngClass]="isActiveNodeChart(chart) ? 'bg-white text-zinc-950 shadow-sm dark:bg-zinc-950 dark:text-zinc-50' : 'text-zinc-500 hover:bg-white/70 hover:text-zinc-900 dark:text-zinc-400 dark:hover:bg-zinc-950/70 dark:hover:text-zinc-100'")
                                span(class="block truncate") {{nodeChartTitle(chart)}}
                                span(class="mt-0.5 block truncate text-[11px] font-medium opacity-75") {{nodeChartSubtitle(chart)}}
                        div(*ngIf="activeNodeChart() as chart", class="rounded-md border border-zinc-200 bg-white dark:border-zinc-800 dark:bg-zinc-900")
                            div(class="border-b border-zinc-200 px-4 py-3 dark:border-zinc-800")
                                h3(class="truncate text-sm font-semibold text-zinc-950 dark:text-zinc-50") {{nodeChartTitle(chart)}}
                                p(class="mt-0.5 truncate text-xs text-zinc-500 dark:text-zinc-400") {{nodeChartSubtitle(chart)}}
                            div(class="p-4")
                                div(*ngIf="nodeChartRows(chart).length === 0", class="flex h-56 items-center justify-center rounded-md border border-dashed border-zinc-300 text-sm text-zinc-500 dark:border-zinc-700 dark:text-zinc-400") 선택한 기간에 표시할 자원 기록이 없습니다.
                                div(*ngIf="nodeChartRows(chart).length > 0", class="grid grid-cols-1 gap-4")
                                    div
                                        h4(class="text-xs font-semibold text-sky-700 dark:text-sky-300") CPU
                                        div([attr.data-node-resource-chart]="chart.node_id", data-node-resource-metric="cpu", class="mt-1 h-52")
                                    div
                                        h4(class="text-xs font-semibold text-emerald-700 dark:text-emerald-300") Memory
                                        div([attr.data-node-resource-chart]="chart.node_id", data-node-resource-metric="memory", class="mt-1 h-52")
                                    div
                                        h4(class="text-xs font-semibold text-amber-700 dark:text-amber-300") Storage
                                        div([attr.data-node-resource-chart]="chart.node_id", data-node-resource-metric="storage", class="mt-1 h-52")
