nav(class="flex flex-wrap items-center justify-end gap-1", aria-label="페이지네이션")
    button(type="button", title="처음 페이지", aria-label="처음 페이지", (click)="move(1)", [disabled]="currentPage <= 1", class="inline-flex h-8 w-8 items-center justify-center rounded-md border border-zinc-200 bg-white text-xs font-semibold text-zinc-600 transition hover:bg-zinc-50 disabled:cursor-not-allowed disabled:opacity-40 dark:border-zinc-700 dark:bg-zinc-900 dark:text-zinc-300 dark:hover:bg-zinc-800")
        i(class="fa-solid fa-angles-left")
    button(type="button", title="이전 페이지", aria-label="이전 페이지", (click)="move(currentPage - 1)", [disabled]="currentPage <= 1", class="inline-flex h-8 w-8 items-center justify-center rounded-md border border-zinc-200 bg-white text-xs font-semibold text-zinc-600 transition hover:bg-zinc-50 disabled:cursor-not-allowed disabled:opacity-40 dark:border-zinc-700 dark:bg-zinc-900 dark:text-zinc-300 dark:hover:bg-zinc-800")
        i(class="fa-solid fa-chevron-left")
    div(class="hidden flex-wrap items-center gap-1 sm:flex")
        button(*ngFor="let page of list", type="button", (click)="move(page)", class="inline-flex h-8 min-w-8 items-center justify-center rounded-md border px-2 text-xs font-semibold transition", [ngClass]="page === currentPage ? 'border-zinc-950 bg-zinc-950 text-white dark:border-zinc-100 dark:bg-zinc-100 dark:text-zinc-950' : 'border-zinc-200 bg-white text-zinc-600 hover:bg-zinc-50 dark:border-zinc-700 dark:bg-zinc-900 dark:text-zinc-300 dark:hover:bg-zinc-800'")
            | {{page}}
    span(class="inline-flex h-8 items-center rounded-md border border-zinc-200 bg-zinc-50 px-2 text-xs font-semibold text-zinc-600 dark:border-zinc-700 dark:bg-zinc-950 dark:text-zinc-300 sm:hidden") {{currentPage}} / {{endPage}}
    button(type="button", title="다음 페이지", aria-label="다음 페이지", (click)="move(currentPage + 1)", [disabled]="currentPage >= endPage", class="inline-flex h-8 w-8 items-center justify-center rounded-md border border-zinc-200 bg-white text-xs font-semibold text-zinc-600 transition hover:bg-zinc-50 disabled:cursor-not-allowed disabled:opacity-40 dark:border-zinc-700 dark:bg-zinc-900 dark:text-zinc-300 dark:hover:bg-zinc-800")
        i(class="fa-solid fa-chevron-right")
    button(type="button", title="끝 페이지", aria-label="끝 페이지", (click)="move(endPage)", [disabled]="currentPage >= endPage", class="inline-flex h-8 w-8 items-center justify-center rounded-md border border-zinc-200 bg-white text-xs font-semibold text-zinc-600 transition hover:bg-zinc-50 disabled:cursor-not-allowed disabled:opacity-40 dark:border-zinc-700 dark:bg-zinc-900 dark:text-zinc-300 dark:hover:bg-zinc-800")
        i(class="fa-solid fa-angles-right")
